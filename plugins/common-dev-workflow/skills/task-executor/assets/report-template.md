# 실행 리포트: {프로젝트명}

**생성일**: {YYYY-MM-DD HH:MM}
**태스크 파일**: {파일 경로}

---

## 요약

| 항목 | 값 |
|------|-----|
| 총 태스크 | {N}개 |
| 성공 | {성공}개 ({성공률}%) |
| 실패 | {실패}개 |
| 스킵 | {스킵}개 |
| 총 소요 시간 | {시간}분 |

### 진행률

```
████████████████░░░░ {완료}/{전체} ({진행률}%)
```

---

## Wave별 결과

| Wave | 태스크 | 성공 | 실패 | 스킵 | 소요 시간 |
|------|--------|------|------|------|----------|
| 1 | {n} | {s} | {f} | {k} | {t}분 |
| 2 | {n} | {s} | {f} | {k} | {t}분 |
| 3 | {n} | {s} | {f} | {k} | {t}분 |
| 4 | {n} | {s} | {f} | {k} | {t}분 |

---

## 성공한 태스크

| ID | 제목 | 에이전트 | 소요 시간 |
|----|------|---------|----------|
| T-001 | {제목} | backend-architect | 8분 |
| T-002 | {제목} | frontend-architect | 5분 |
| ... | ... | ... | ... |

---

## 실패한 태스크

### T-{ID}: {제목}

| 항목 | 내용 |
|------|------|
| 에러 유형 | {검증 실패 / 실행 에러 / 타임아웃} |
| 에러 메시지 | `{에러 내용}` |
| 시도 횟수 | {n}/2 |
| 롤백 | {실행됨 / 미실행 / 없음} |
| 영향받은 태스크 | T-{의존 태스크들} |

**에러 상세**:
```
{스택 트레이스 또는 상세 에러 로그}
```

**권장 조치**:
1. {수정 방법 제안}
2. `/task-executor --resume` 로 재시도

---

## 스킵된 태스크

| ID | 제목 | 스킵 사유 |
|----|------|----------|
| T-{ID} | {제목} | 의존성 T-{선행} 실패 |
| ... | ... | ... |

---

## 검증 결과

### Unit Tests
- [{x/!}] 모든 새 함수에 단위 테스트 존재
- [{x/!}] 테스트 전체 통과: `{명령어}`
- [{x/!}] 커버리지 감소 없음

### Integration Tests
- [{x/!}] API 연동 테스트 통과
- [{x/!}] 데이터 흐름 검증 완료

### System Tests
- [{x/!}] 빌드 성공: `{명령어}`
- [{x/!}] 린트 통과: `{명령어}`
- [{x/!}] 기존 테스트 회귀 없음

---

## 에이전트 사용 통계

| 에이전트 타입 | 사용 횟수 | 성공률 | 평균 시간 |
|--------------|----------|--------|----------|
| backend-architect | {n} | {%} | {t}분 |
| frontend-architect | {n} | {%} | {t}분 |
| test-automator | {n} | {%} | {t}분 |
| general-purpose | {n} | {%} | {t}분 |

---

## 타임라인

```
{HH:MM} 실행 시작
{HH:MM} Wave 1 시작 (4개 병렬)
{HH:MM} Wave 1 완료
{HH:MM} Wave 2 시작 (4개 병렬)
{HH:MM} T-005 실패 - 재시도 1/2
{HH:MM} T-005 실패 - 재시도 2/2
{HH:MM} T-005 스킵 처리
{HH:MM} Wave 2 완료
{HH:MM} Wave 3 시작
{HH:MM} Wave 3 완료
{HH:MM} Verification 시작
{HH:MM} 실행 완료
```

---

## 권장 조치

### 즉시 조치 필요
1. **T-{ID}**: {실패 태스크 수정 방법}
2. ...

### 선택적 조치
1. {개선 제안}
2. {최적화 제안}

### 다음 단계
```bash
# 실패 태스크 수정 후 재개
/task-executor --resume "{태스크 파일}"

# 또는 새로 시작
/task-executor "{태스크 파일}"
```

---

## 메타데이터

```yaml
executor_version: 1.0
started_at: {ISO timestamp}
completed_at: {ISO timestamp}
total_agents_spawned: {n}
max_parallel_agents: 5
```
